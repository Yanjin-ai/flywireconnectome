# Exact versions used to reproduce the published results (tested on Python 3.14;
# these pins require Python >= 3.11). For a looser install on Python >= 3.10 use
# `pip install -e .` instead, which uses the floor constraints in setup.py.
pandas==2.3.3
numpy==2.3.5
networkx==3.6.1
pyarrow==23.0.0
scipy==1.17.1
matplotlib==3.10.8
seaborn==0.13.2
pulp==3.3.2
pytest==9.0.3
