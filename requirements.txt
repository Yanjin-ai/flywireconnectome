# Compatibility floors — resolves on Python 3.10–3.14, including whatever
# default Python Streamlit Community Cloud offers (pip picks matching wheels).
# For the EXACT versions used to produce the published results, see
# requirements-lock.txt (`pip install -r requirements-lock.txt`).
pandas>=2.0
numpy>=1.24
networkx>=3.0
pyarrow>=14
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
pulp>=2.7
pytest>=7.0
streamlit>=1.30          # for src/explorer_app.py
# optional (tighter MIS upper bound via Lovász theta SDP): cvxpy>=1.4
# optional (Neuroglancer short links, FlyWire auth): fafbseg, nglui, caveclient
